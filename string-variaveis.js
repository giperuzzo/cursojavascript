// declarando variáveis nao é mais usual usar var pq conseguimos acessar ela fora do escopo. vamos usar const e let # entre as duas, qdo declaramos variaveis com let conseguimos alterar ela depois já a cons como o proprio nome diz é constante nao conseguimos alterar
// Strings = tudo que for texto - e vamos usar o length qu vai identificar qtos caracteres essa variavel tem ex message
// const message = 'Hello World!'

// console.log(message.length)


//  modo antigo console.log(`Meu nome é ` + firstName + ' ' + lastName)
// template string modo mais recente de mostrar o qeu queremos impresso
// toUpperCase depoois da variavel deixa tudo em minusculo e o toLowerCase  deixa tudo em maisculo

// const firstName = 'Gislene'
// const lastName = 'Peruzzo'
// console.log(`Meu nome é ${firstName.toUpperCase} ${lastName.toLowerCase}`)

// usando split

// const names = "Gislene, Camila, Monique"
// console.log(names.split(" "))
// site recomendado para metodos para strings mdn web docs

// numeros podemos somar diminuir entre outras operaçoes e se usar o metodo toString transforma o numero em string
// método typeof verifica se ele é uma string
// const number = 5;
// console.log(number.toString())
// console.log(typeof number.toString());

// Booleanos
false
true
console.log(2==3)

// Null = vazia e Undefined = indefinida

const x = null;
const y = undefined;

